import 'Person.dart';
import 'Square.dart';
import 'Circle.dart';
import 'Shape.dart';

void main() {
  // List<Person> persons;
  // Person person = Person("NS", 20);
  // Person person1 = Person("NSS", 21);
  // Person person2 = Person("NSSS", 22);

  // person.name = "NS";
  // person.age = 20;

  // print(person.name);
  // print(person.age);
  // person.introduce();
  // print(person.birthday());
  // print(Person.count);

  // Encapsulation
  // Person p = Person("NS", 10);
  // print(p.getName());
  // print(p.getAge());

  // Abstraction
  // Square s = Square(10);
  // Shape s = Square(10);
  // print(s.getArea());

  // Polymorphism
  // Square s = Square(10);
  // Circle c = Circle(10);
  // List <Shape> shapes = [s, c];
}


// OOP Pillars
// 1. Encapsulation
// Add _ (underscore) in front of the variable's name

// 2. Abstraction
// Add abstract in front of the class' name

// 3. Inheritance
// Add extends after the class' name

// 4. Polymorphism